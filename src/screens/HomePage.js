import React from "react"
import { MenuContainer, Home } from "../components"

export const HomePage = () => {
    return (
        <>
            <MenuContainer />
            <Home />
        </>
    )
}